# Generated by .github/workflows/update-requirements.yml.
# Contains the optional healpy support stack. Run the workflow or
# `poetry export --format requirements.txt --output requirements-healpy.txt --extras healpy_support --without-hashes`
# to refresh pinned dependencies after changing pyproject.toml or poetry.lock.
