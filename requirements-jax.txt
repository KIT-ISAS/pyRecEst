# Generated by .github/workflows/update-requirements.yml.
# Contains the optional JAX backend stack. Run the workflow or
# `poetry export --format requirements.txt --output requirements-jax.txt --extras jax_support --without-hashes`
# to refresh pinned dependencies after changing pyproject.toml or poetry.lock.
