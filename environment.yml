---
name: pyRecEst
channels:
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - matplotlib
  - mpmath
  - pytest
  - shapely
  - pip
  - pip: # Installing them via pip is much faster than using the conda-forge channel
      - filterpy
      - healpy
      - pyshtools
      - parameterized
      - beartype
      - numpy-quaternion
