# Paper Artifact Template

Replace this template with the title, citation, and reproduction instructions
for the paper or report.

Suggested commands:

```bash
python -m pip install -e .
python run_experiment.py --output results.json
```

The expected results in `expected_results.json` should include tolerances for
stochastic or backend-dependent values.
