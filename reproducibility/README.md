# Reproducibility Artifacts

Use this directory for experiment artifacts tied to papers, reports, or
benchmark claims. A reproducible artifact should include the exact PyRecEst
version or commit, dependency lock information, input configuration, expected
outputs, and a one-command reproduction script.

Start new artifacts from `templates/paper-artifact/`.
