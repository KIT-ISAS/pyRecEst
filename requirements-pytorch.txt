# Generated by .github/workflows/update-requirements.yml.
# Contains the optional PyTorch backend stack. Run the workflow or
# `poetry export --format requirements.txt --output requirements-pytorch.txt --extras pytorch_support --without-hashes`
# to refresh pinned dependencies after changing pyproject.toml or poetry.lock.
